import { Menu, MenuComponent } from "./Menu";

export default class MenuSelectService {
    select(menu: Menu): MenuComponent {
        // TODO: make this function by using inquirer
        throw new Error("Method not implemented.");
    }
}